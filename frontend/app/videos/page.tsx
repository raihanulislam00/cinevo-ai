"use client";
import { Search, SlidersHorizontal } from "lucide-react"; import { useState } from "react"; import { AppShell } from "@/components/layout/app-shell"; import { VideoCard } from "@/components/videos/video-card"; import { useVideos } from "@/hooks/use-data"; import type { VideoStatus } from "@/types"; import Link from "next/link";
export default function VideosPage() { const [search, setSearch] = useState(""); const [status, setStatus] = useState(""); const { data, isLoading } = useVideos({ search, status }); const items = data?.items ?? []; return <AppShell><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-sm font-semibold text-[#0d8478]">Your archive</p><h1 className="display mt-2 text-4xl font-bold text-[#173b39]">My videos</h1><p className="mt-2 text-[#7b8983]">Every idea you&apos;ve brought to life.</p></div><Link href="/create" className="rounded-full bg-[#ee7967] px-5 py-3 text-sm font-bold text-white">Create video <span aria-hidden="true">↗</span></Link></div><div className="mt-9 flex flex-col gap-3 sm:flex-row"><label className="relative flex-1"><Search className="absolute left-4 top-3.5 h-4 w-4 text-[#9aa6a1]" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search your videos" className="input pl-11" /></label><label className="relative sm:w-48"><SlidersHorizontal className="absolute left-4 top-3.5 h-4 w-4 text-[#9aa6a1]" /><select value={status} onChange={(event) => setStatus(event.target.value)} className="input appearance-none pl-11"><option value="">All statuses</option>{(["Pending", "Processing", "Completed", "Failed", "Cancelled"] as VideoStatus[]).map((item) => <option key={item}>{item}</option>)}</select></label></div>{isLoading ? <div className="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-3"><div className="h-80 animate-pulse rounded-2xl bg-[#e6eee9]" /><div className="h-80 animate-pulse rounded-2xl bg-[#e6eee9]" /></div> : items.length ? <div className="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{items.map((video) => <VideoCard key={video.id} video={video} />)}</div> : <div className="mt-8 rounded-3xl border border-dashed border-[#cfdcd5] bg-white px-6 py-20 text-center"><h2 className="display text-3xl font-bold text-[#173b39]">No videos yet.</h2><p className="mt-2 text-[#7b8983]">Your first video is only one idea away.</p><Link href="/create" className="mt-6 inline-block rounded-full bg-[#173b39] px-5 py-3 text-sm font-bold text-white">Create your first video</Link></div>}</AppShell>; }