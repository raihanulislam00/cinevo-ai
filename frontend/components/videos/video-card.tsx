import Link from "next/link"; import { ArrowUpRight, Clock3, Film } from "lucide-react"; import type { Video } from "@/types"; import { formatDate, formatDuration } from "@/lib/utils"; import { VideoStatus } from "./video-status";
export function VideoCard({ video }: { video: Video }) { const duration = video.videoPlan?.duration || video.scenes?.reduce((sum, scene) => sum + scene.duration, 0) || 0; return <Link href={`/videos/${video.id}`} className="group overflow-hidden rounded-2xl border border-[#e4e8e3] bg-white transition hover:-translate-y-1 hover:shadow-xl hover:shadow-[#173b39]/8"><div className="relative aspect-[1.45] overflow-hidden bg-[#d9eee7]"><div className="grain absolute inset-0 opacity-60" /><div className="absolute bottom-5 left-5 right-5"><div className="mb-2 flex items-center gap-2 text-xs font-bold uppercase tracking-[.15em] text-[#46766d]">{video.videoPlan?.style || "Cinematic"}</div><p className="display max-w-[85%] text-2xl font-bold leading-tight text-[#173b39]">{video.title}</p></div><span className="absolute right-4 top-4 rounded-full bg-white/80 p-2 text-[#173b39] backdrop-blur transition group-hover:bg-[#ee7967] group-hover:text-white"><ArrowUpRight className="h-4 w-4" /></span></div><div className="p-5"><div className="flex items-center justify-between"><VideoStatus status={video.status} /><span className="text-xs text-[#89948f]">{formatDate(video.createdAt)}</span></div><div className="mt-4 flex items-center gap-4 text-xs font-semibold text-[#7e8b86]"><span><Clock3 className="mr-1 inline h-3.5 w-3.5" />{formatDuration(duration)}</span><span><Film className="mr-1 inline h-3.5 w-3.5" />{video.videoPlan?.scenes?.length || video.scenes?.length || 0} scenes</span></div></div></Link>; }