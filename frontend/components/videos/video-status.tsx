import { cn } from "@/lib/utils"; import type { VideoStatus as Status } from "@/types"; export function VideoStatus({ status }: { status: Status }) { return <span className={cn("inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-bold", status === "Completed" && "bg-[#e1f4ea] text-[#187359]", status === "Processing" && "bg-[#fff0d8] text-[#a96816]", status === "Failed" && "bg-[#ffe4df] text-[#ba4f42]", status === "Pending" && "bg-[#e8eef1] text-[#62727a]", status === "Cancelled" && "bg-[#f0e8e6] text-[#806e69]")}><span className="h-1.5 w-1.5 rounded-full bg-current" />{status}</span>; }